  <!DOCTYPE html>
  <!-- ABOUT -->
  <html lang="en">
    <head>
      <link rel="manifest" href="manifest.json">
      <link rel="icon" href="" type="image/x-icon">    
      <link rel="shortcut icon" href="" type="image/x-icon">
      <meta name="description" content="Bits Workshop Order System"/>
      <!--Import Google Icon Font-->
      <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
      <!--Import materialize.css-->
      <link type="text/css" rel="stylesheet" href="../materialize.min.css"  media="screen,projection"/>
      <link type="text/css" rel="stylesheet" href="../allusers-style.css"/>
      <meta name="theme-color" content="black">
      <!--Let browser know website is optimized for mobile-->
      <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
      <script type="text/javascript" src="../min.js"></script>
      <title>Connect</title>
    </head>

    <body>
      <!--Import jQuery before materialize.js-->
      <script type="text/javascript" src="../jquery214.js"></script>
      <script type="text/javascript" src="../materialize.min.js"></script>
      <script type="text/javascript" src="../dashboard-action.js"></script>
      <!-- NAVIGATION -->
      <% include partials/downAdmL1.ejs %>
      <!-- EIGHTY FOUR -->
      <div class="eightyfour grey lighten-2">
        <table class="centered table white">
            <thead>
              <tr>
                  <th>User Name</th>
                  <th>User Id</th>
                  <th>Last Login</th>
                  <th>Last Logout</th>
                  <th>See Details</th>
              </tr>
            </thead>

            <tbody>
              <% for(var i=0; i < data.length; i++) { if(data[i].type=="worker"){ %>
                <tr>
                  <td><%= data[i].username%></td>
                  <td><%= data[i]._id %></td>
                  <td><% if(data[i].login[data[i].login.length-1]){ %><%= new Date(data[i].login[data[i].login.length-1]).toLocaleDateString('en-GB') %> <%= new Date(data[i].login[data[i].login.length-1]).toLocaleTimeString('en-GB') %><% } %></td>
                  <td><% if(data[i].logout[data[i].logout.length-1]){ %><%= new Date(data[i].logout[data[i].logout.length-1]).toLocaleDateString('en-GB') %> <%= new Date(data[i].logout[data[i].logout.length-1]).toLocaleTimeString('en-GB') %><% } %></td>
                  <td><a href="./allworkers/<%= data[i]._id %>">Click Here</a></td>
                 </tr>
              <% }} %>
            </tbody>
          </table>
      </div>
      <!-- FOOTER -->
      <% include partials/footer.ejs %>
    </body>
  </html>
